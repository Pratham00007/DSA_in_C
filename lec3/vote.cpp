#include <iostream>
using namespace std;
int main(){
    int age;
    cout << "Enter Your age";
    cin>>age;
    if (age>=18){
        cout<<"Can vote";
    }else{
        cout << "Can't Vote";
    }

    // if there is a single statement to rn in condition you can remove the curly braces
    // but good prgmmers not do that 
}